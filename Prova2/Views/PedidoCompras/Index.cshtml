@model IEnumerable<Prova2.PedidoCompra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fornecedor1.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Material1.descricao)
        </th>
        <!--
    <th>
        @Html.DisplayNameFor(model => model.PrecoCompra.preco)
    </th>
        -->
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Quantidade)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Fornecedor1.Nome)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Material1.descricao)
    </td>
    <!--
    <td>
        @Html.DisplayFor(modelItem => item.PrecoCompra.preco)
    </td>
    -->
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.idPedCompra }) |
        @Html.ActionLink("Details", "Details", new { id = item.idPedCompra }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.idPedCompra })
    </td>
</tr>
}

</table>
