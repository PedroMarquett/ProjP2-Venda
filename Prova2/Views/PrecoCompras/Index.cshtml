@model IEnumerable<Prova2.PrecoCompra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.preco)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Material.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.preco)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Material.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPrecoCompra }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPrecoCompra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPrecoCompra })
        </td>
    </tr>
}

</table>
